const Home = () => {
  return (
    <div>
      <p>
        NEXT JS APP DIRECTORY - TRPC - PRISMA ORM - TAILWIND CSS - SHADCN UI -
        NEXT AUTH - NEXT THEMES
      </p>
    </div>
  );
};

export default Home;
